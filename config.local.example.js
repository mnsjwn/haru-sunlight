/* =========================================================
   기상청 서비스키 — 로컬 전용 설정 (템플릿)

   사용법
   1. 이 파일을 같은 폴더에 config.local.js 라는 이름으로 복사한다.
   2. 아래 따옴표 안에 공공데이터포털에서 발급받은 서비스키를 붙여넣는다.
      (기상청_단기예보 조회서비스 + 기상청_생활기상지수 조회서비스,
       두 개를 활용신청하면 같은 키를 함께 쓴다)
   3. 페이지를 새로고침한다.

   config.local.js는 .gitignore에 등록돼 있어 깃허브에 올라가지 않는다.
   이 example 파일만 커밋되고, 실제 키가 든 config.local.js는 각자 로컬에만 둔다.
   ========================================================= */
window.KMA_SERVICE_KEY = '';
